<UserControl x:Class="CManager.Presentation.GuiApp.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CManager.Presentation.GuiApp.Views"
             xmlns:viewmodels="clr-namespace:CManager.Presentation.GuiApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d"
             Background="#222831"
             d:DesignHeight="700" d:DesignWidth="400">
    
        <Grid>
            <StackPanel VerticalAlignment="Center">
                <Image Source="../Resources/Images/ImageByChatGPT1.png" Height="320" Margin="0 0 0 20" />
                <Button x:Name="CreateCustomer_Button" Content="Create Customer" Style="{StaticResource Standard_Buttons}" Command="{Binding GoToCreateCustomerCommand}"/>
                <Button x:Name="DisplayCustomers_Button" Content="Display All Customers" Style="{StaticResource Standard_Buttons}" Command="{Binding GoToDisplayCustomersCommand}"/>
            </StackPanel>
        </Grid>
   
</UserControl>
