<Window x:Class="CManager.Presentation.GuiApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CManager.Presentation.GuiApp"
        xmlns:views="clr-namespace:CManager.Presentation.GuiApp.Views"
        xmlns:viewmodels="clr-namespace:CManager.Presentation.GuiApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="820" Width="400">

    <Border Background="#222831" CornerRadius="25">
        <Grid>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
                <RowDefinition Height="700" />
            </Grid.RowDefinitions>

            <Grid x:Name="TopBar" Grid.Row="0" Background="Transparent" MouseDown="TopBar_MouseDown">
                <TextBlock x:Name="TopBar_Text" Style="{StaticResource TopBar_Text}">
                    <Run Text="CManager"/>
                </TextBlock>
                <Button Click="ExitButton_Click" Style="{StaticResource ExitButton}" />
            </Grid>

            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
                        <views:HomeView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:CreateCustomerViewModel}">
                        <views:CreateCustomerView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:DisplayInfoOneCustomerViewModel}">
                        <views:DisplayInfoOneCustomerView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:DisplayAllCustomersViewModel}">
                        <views:DisplayAllCustomersView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:EditCustomerViewModel}">
                        <views:EditCustomerView />
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
    </Border>
</Window>
